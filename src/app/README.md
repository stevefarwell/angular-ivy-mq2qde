## - Using this data: https://jsonplaceholder.typicode.com/users please do the following:

### 1 Display the data in the browser. From each user display: id, name, username and email, each user's data must be inside a card, one next to the other. For styles don't use any framework e.g. Bootstrap. Cards example: https://getbootstrap.com/docs/4.0/components/card/

### 1.1 At the top of the boxes display the total of elements in the array. 

### 2 Add a input box and a button. Clicking on the button should return the users which username matches the value in the input box. 

### 3 Add a button/link to the boxes that will delete the entry. The total of items showed at the top should be updated.

